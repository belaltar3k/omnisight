kind = "web"
previewPath = "/"
title = "Surveillance Dashboard"
version = "1.0.0"
id = "artifacts/surveillance-dashboard"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19995

[services.development]
run = "pnpm --filter @workspace/surveillance-dashboard run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/surveillance-dashboard", "run", "build" ]
publicDir = "artifacts/surveillance-dashboard/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19995"
BASE_PATH = "/"
