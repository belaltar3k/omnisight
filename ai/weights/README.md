# Model Weights

This directory contains the serialized model weights (`.pt`, `.pth` files) for the various AI components.

## Directory Structure

- `crime_skelnet/`: Checkpoints and pre-trained weights for the Crime SkelNet model.
- `video_mae/`: Checkpoints and pre-trained weights for the Video MAE model.

